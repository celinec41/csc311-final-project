\documentclass[11pt, letterpaper]{article}
% Packages
\usepackage[utf8]{inputenc}
\usepackage[margin=1in]{geometry}
\usepackage{amsmath, amssymb}
\usepackage{tikz}
\usepackage{booktabs}
\usepackage{float}
\usepackage{graphicx}
% TikZ libraries
\usetikzlibrary{positioning, arrows.meta}
% Formatting
\setlength{\parindent}{0pt}
\setlength{\parskip}{0.5em}
\begin{document}
\begin{center}
    {\LARGE \textbf{CSC311 Summer 2026}} \\[0.5em]
    {\large \textbf{Final Project}} \\[0.5em]
    {\normalsize Xiaohan Ma, Meixuan Lou, Siyi Chen} \\
    {\normalsize Due: Friday, August 7, 2026 before 11:00pm EST}
\end{center}
\vspace{1em}

% ============================================================
\section*{1. k-Nearest Neighbors}

\subsection*{(a) Validation accuracy vs. $k$ (user-based)}
% TODO: insert plot, report accuracy for k in {1,6,11,16,21,26}

\subsection*{(b) Best $k^*$ and test accuracy}
% TODO: state chosen k* and final test accuracy

\subsection*{(c) Item-based collaborative filtering}
% TODO: state underlying assumption for item-based kNN, repeat (a)-(b)

\subsection*{(d) User- vs. item-based comparison}
% TODO: compare test performance, state which is better

\subsection*{(e) Limitations of kNN}
% TODO: list at least two limitations

% ============================================================
\section*{2. Item Response Theory}

\subsection*{(a) Derivation}

Let $C \in \{0,1\}^{N \times M}$ denote the sparse matrix of $N$ students and $M$ questions, observed only on the index set $O$. The probability that student $i$ answers question $j$ correctly is
\[
p(c_{ij} = 1 \mid \theta_i, \beta_j) = \sigma(\theta_i - \beta_j) = \frac{\exp(\theta_i - \beta_j)}{1 + \exp(\theta_i - \beta_j)}.
\]

Assuming independence across observed entries, the log-likelihood is
\[
\log p(C \mid \theta, \beta) = \sum_{(i,j) \in O} \Big[ c_{ij}(\theta_i - \beta_j) - \log\big(1 + \exp(\theta_i - \beta_j)\big) \Big].
\]

Taking derivatives:
\[
\frac{\partial \log p(C\mid\theta,\beta)}{\partial \theta_i} = \sum_{j : (i,j) \in O} \Big[ c_{ij} - \sigma(\theta_i - \beta_j) \Big],
\]
\[
\frac{\partial \log p(C\mid\theta,\beta)}{\partial \beta_j} = -\sum_{i : (i,j) \in O} \Big[ c_{ij} - \sigma(\theta_i - \beta_j) \Big].
\]

\subsection*{(b) Training curve}
% TODO: insert hyperparameters and plot of train/val log-likelihood vs. iteration

\subsection*{(c) Final accuracy}
% TODO: report validation and test accuracy

\subsection*{(d) Probability curves}
% TODO: plot p(c_ij = 1) vs theta for three chosen questions, comment on shape

% ============================================================
\section*{3. Matrix Factorization / Neural Network}
% Delete whichever option your group did not use

\subsection*{(a) SVD reconstruction}
% TODO: try 5+ values of k, report validation-selected k and test accuracy

\subsection*{(b) Limitation of SVD}
% TODO: discuss how missing entries are handled

\subsection*{(c)--(e) ALS with SGD}
% TODO: describe implementation, chosen hyperparameters (lr, iterations, k*),
% loss curve plot, final validation and test accuracy

% --- OR, if Neural Network option ---
\subsection*{(a) ALS vs. neural network differences}
% TODO: at least three differences

\subsection*{(b) Autoencoder implementation}
% TODO: describe forward pass

\subsection*{(c)--(d) Training and results}
% TODO: k sweep {10,50,100,200,500}, chosen k*, hyperparameters,
% loss/accuracy curve, final test accuracy

\subsection*{(e) L2 regularization}
% TODO: sweep lambda in {0.001, 0.01, 0.1, 1}, report final val/test accuracy,
% discuss whether regularization helped

% ============================================================
\section*{4. Ensemble}
% TODO: describe bagging process (3 base models, bootstrapped training sets),
% report validation and test accuracy, discuss whether ensembling helped and why

\end{document}
